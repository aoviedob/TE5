version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
    overwrite: true
hooks:
  AfterInstall:
    - location: start.sh
      timeout: 100000
      runas: root
  ApplicationStop:
    - location: stop.sh
      timeout: 100000
